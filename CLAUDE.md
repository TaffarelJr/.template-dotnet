# CLAUDE.md

@AGENTS.md

## Claude Code

- Path-scoped rules live in [.github/instructions/][instructionsFolder],
  one file per file type.
  They are not loaded automatically, so read the one that matches
  what you are editing.
- Everything in [docs/][docsFolder] is reference material.
  Read a document when a rule above is ambiguous, not by default.

<!-- Source Code URIs (alphabetical by file hierarchy) -->

[instructionsFolder]: ./.github/instructions/
[docsFolder]: ./docs/
